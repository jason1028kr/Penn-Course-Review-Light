<%= form_with(model: course_review, local: true) do |form| %>
  <% if course_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_review.errors.count, "error") %> prohibited this course_review from being saved:</h2>

      <ul>
      <% course_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :course_review_content %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id, id: :course_review_course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
