<%= form_with(model: professor_review, local: true) do |form| %>
  <% if professor_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professor_review.errors.count, "error") %> prohibited this professor_review from being saved:</h2>

      <ul>
      <% professor_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :professor_review_content %>
  </div>

  <div class="field">
    <%= form.label :professor_id %>
    <%= form.text_field :professor_id, id: :professor_review_professor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
